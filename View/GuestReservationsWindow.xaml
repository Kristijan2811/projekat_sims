<Window x:Class="BookingApp.View.GuestReservationsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Moje rezervacije" Height="430" Width="750"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
	<DockPanel Margin="10">

		<!-- Gornji deo: filter -->
		<StackPanel DockPanel.Dock="Top"
                    Orientation="Horizontal"
                    Margin="0,0,0,10">
			<TextBlock Text="Filtriraj po statusu:"
                       VerticalAlignment="Center"
                       Margin="0,0,10,0"/>
			<ComboBox Width="150"
                      SelectedValue="{Binding SelectedFilter}"
                      SelectedValuePath="Content">
				<ComboBoxItem Content="Sve"/>
				<ComboBoxItem Content="Na cekanju"/>
				<ComboBoxItem Content="Potvrdjene"/>
				<ComboBoxItem Content="Odbijene"/>
			</ComboBox>
		</StackPanel>

		<!-- Donji deo: dugme za otkazivanje -->
		<StackPanel DockPanel.Dock="Bottom"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,10,0,0">
			<Button Content="Otkazi izabranu rezervaciju"
                    Width="200" Height="30"
                    Click="CancelReservationButton_Click"/>
		</StackPanel>

		<!-- Glavni deo: lista rezervacija -->
		<DataGrid ItemsSource="{Binding Reservations}"
                  SelectedItem="{Binding SelectedReservation}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False">
			<DataGrid.Columns>
				<DataGridTextColumn Header="Datum" Binding="{Binding Date, StringFormat=\{0:yyyy-MM-dd\}}" />
				<DataGridTextColumn Header="Hotel" Binding="{Binding HotelName}" />
				<DataGridTextColumn Header="Apartman" Binding="{Binding ApartmentName}" />
				<DataGridTextColumn Header="Status" Binding="{Binding StatusText}" />
				<DataGridTextColumn Header="Razlog odbijanja" Binding="{Binding RejectionReason}" />
			</DataGrid.Columns>
		</DataGrid>

	</DockPanel>
</Window>
